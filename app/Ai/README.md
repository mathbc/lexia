# AI

Pasta de convenção do `laravel/ai`: `php artisan make:agent` e
`php artisan make:tool` geram em `app/Ai/Agents` e `app/Ai/Tools`.

Vazio até o módulo de Jurisprudência. O pipeline de recuperação vive em
`app/Rag`.
